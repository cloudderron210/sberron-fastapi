services:
  db:
    image: postgres 
    restart: always
    environment:
      POSTGRES_USER: derron
      POSTGRES_PASSWORD: password
      POSTGRES_DB: debug2
    ports:
      - "5432:5432"



      
